//Template Literals

//Use ` (the key near Esc or 1. not the near ENTER) between the string to "activate" this feature.
//Use ${} in variables or numbers to insert in the string.

//E.g.

let product = "Artichoke";
let price = 2.25;
let qty = 5;

console.log(
  `You bought ${qty} of ${product}(s). Total price is $${price * qty}`
);
